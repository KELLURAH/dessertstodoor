import 'package:flutter/material.dart';

Drawer Sidebar({required String email, required String name}) {
  return Drawer(
    elevation: 0.35,
    child: ListView(
      // padding: const EdgeInsets.zero,
    ),
  );
}
